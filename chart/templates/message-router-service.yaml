apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-message-router
  labels:
    app.kubernetes.io/name: message-router
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: message-router
    app.kubernetes.io/instance: {{ .Release.Name }}
